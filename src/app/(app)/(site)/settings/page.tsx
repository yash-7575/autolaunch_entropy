import { SettingsComponent } from '@/components/settings/settings.component';

export const metadata = { title: 'AutoLaunch Settings' };

export default function SettingsPage() {
  return <SettingsComponent />;
}
