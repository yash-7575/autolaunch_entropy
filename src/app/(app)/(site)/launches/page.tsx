import { LaunchesComponent } from '@/components/launches/launches.component';

export const metadata = { title: 'AutoLaunch Calendar' };

export default function LaunchesPage() {
  return <LaunchesComponent />;
}
